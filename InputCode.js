
function alo () {
    return 1
}

/**
let array = []

array.forEach(function (ome) {
    ome+1
})


function SomeCalc(a,b,c) {
    return a + b - c
}
// some
var x = 1;
let y = 3;
const z = 2;
function bar(ff) {
    ff = ff || undefined
    if (ff) return ff 
    if (x < y) {
       y = SomeCalc(x,y,z);
    } else {
        x = y-1;
        if (x = y) {
            return x | ff;
        }
    }
    for (var i = 0; i < 2; i++) {
        x += i;
    }
    bar();
}
bar()
for (var i = 0; i < 4; i++) {
    x += i;
}
if (x < y) {
    y = SomeCalc(x,y,z);
 } else {
     x = y;
 }


const somne = bar()
console.log(somne);

 */